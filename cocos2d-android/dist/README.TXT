In eclipse, the .jardesc files can be used (or reconfigured) to build JAR files that we can publish for release.
We may want to switch this to a custom ant build if release/distribution builds/versioning gets more complicated.

Ray Cardillo [2010-JAN-13]